use json to decide css color or font...

for example: 

```json
{
    "work":{
        "color":"#1e1e1e",
        ...
        "file":{
            "color":"red"
        }
    }
}
```

可以依照[css](../ui/css/)中对应不同内容去定义json用来手动改变内容